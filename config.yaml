server:
  port: 8080

database:
  host: "localhost"
  port: 5433
  user: "postgres"
  password: "password"
  dbname: "notification_db"

kafka:
  brokers:
    - "localhost:9092"
  topic: "notifications.events"
